{
  "name": "watch-finder",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "turbo run build --no-daemon",
    "dev": "turbo run dev --no-daemon",
    "start": "turbo run start --no-daemon",
    "db:gen": "turbo run db:gen --no-daemon",
    "db:push": "turbo run db:push --no-daemon",
    "db:pull": "turbo run db:pull --no-daemon"
  },
  "devDependencies": {
    "@types/react-date-range": "^1.4.4",
    "prettier": "latest",
    "prisma": "^4.15.0",
    "turbo": "^1.9.3"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "@prisma/client": "^4.15.0",
    "bootstrap": "^5.3.0",
    "date-fns": "^2.30.0",
    "react": "^18.2.0",
    "react-bootstrap": "^2.7.4",
    "react-date-range": "^1.4.0"
  },
  "packageManager": "yarn@1.22.15"
}
